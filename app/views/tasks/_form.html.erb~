
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div id="bodao">
<body class="bg-darkTeal">

 <div class="catask-form padding20 block-shadow">
<span class="m-btn blue-stripe">Cadastrar Atividades</span>
            <hr class="thin"/>
            <br />
            <%= nested_form_for @task do |f| %>
<div class="treeview" data-role="treeview">
<ul>
<div class="input-control text full-size" data-role="input">
<%= f.grouped_collection_select :projeto_id, Cliente.order(:name), :projetos, :name, :id, :name, prompt: "Projeto" %> 
 </div>

<ul>

  
<div class="field>
<%= f.label :parent_id %><br>
<%= f.select :parent_id, nested_set_options(Task){|i| "#{'-' * i.level} #{i.name}" }, { select: @task.parent_id, include_blank: true} %>
</div>
 
<li>

 
</li>



<% end %>

<ul>







 



<ul>


</ul>
</ul>
</ul>
</ul>
<br>
</div>
  <br> <br> <br>

 

  <div class="actions">



 
  <%= f.submit 'Cadastrar', class: 'm-btn blue' %>
</div>
<br><% end %>
</div>
</div>

